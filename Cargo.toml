[package]
name = "senba-cache"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.9"
rand_distr = "0.5"

[dev-dependencies]
criterion = { version = "0.5", default-features = false }

[[bench]]
name = "micro"
harness = false
