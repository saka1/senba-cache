[package]
name = "senba-cache"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10"
rand_distr = "0.6"

[dev-dependencies]
criterion = { version = "0.8", default-features = false }

[[bench]]
name = "micro"
harness = false
